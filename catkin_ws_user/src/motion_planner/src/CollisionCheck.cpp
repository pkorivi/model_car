
//#include "MotionPlanner.h"
namespace fub_motion_planner{

  /** Function for obstacle collision check checking
  **
  ** @param msg
  */
/*
  double MotionPlanner::CollisionCheck(std::vector<double> s_pts,std::vector<double> d_pts, std::vector<double> t_pts){
    if (mObstaclesMsg) {
      //Move this to motion planner callback timer
			freezedObstaclesMsg=mObstaclesMsg;
      if(current_vehicle_state.m_obstacle_msg)
        std::cout << "timer " << current_vehicle_state.m_obstacle_msg->obstacles[0].abs_velocity.twist.linear.x << '\n';
      for(const abutonomous_obstacle_msgs::Obstacle & obst : freezedObstaclesMsg->Obstacles ){
        std::cout << " bst vel "<<obst.abs_velocity.twist.linear.x << '\n';

      }

    }

    return 10;
  }//end of collision check
*/

}
